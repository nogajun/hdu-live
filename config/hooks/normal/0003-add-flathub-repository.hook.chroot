#!/bin/sh

set -e

# set flathub repository and install packages
flatpak remote-add --system --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo

# install flatpak packages
flatpak_packages="io.freetubeapp.FreeTube org.luanti.luanti org.libreoffice.LibreOffice org.freecad.FreeCAD org.gtk.Gtk3theme.Breeze"
flatpak install --system --noninteractive $flatpak_packages
